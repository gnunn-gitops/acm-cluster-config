apiVersion: tower.ansible.com/v1alpha1
kind: AnsibleJob
metadata:
  name: cluster-config-pre-notification
  namespace: openshift-gitops
spec:
  job_template_name: Demo Job Template
  extra_vars:
    cluster-config-prehook: true
